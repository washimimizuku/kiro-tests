[package]
name = "rust-calculator"
version = "0.1.0"
edition = "2024"

[dependencies]
rustyline = "14.0"  # For readline functionality (history, editing)
clap = { version = "4.0", features = ["derive"] }  # For command line argument parsing
